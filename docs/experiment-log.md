# Experiment Log

No experiments have been run yet.

Schema contracts, market-session selection, basic label-price validation, and leakage-safe feature
group tests are complete.

Before the first experiment, validate data providers and timestamp availability, implement runtime
normalization and leakage-safe joins, connect actual price rows to the selected sessions, and build the
end-to-end label dataset. Model training should begin only after those checks pass.
